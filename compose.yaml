services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: zaine-dev-portfolio
    env_file:
      - .env.local
    ports:
      - "3000:3000"
    restart: unless-stopped
